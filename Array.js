//arbitrary byte-based data structures to implement network protocols,
// cryptography algorithms, file format manipulations, etc.


var id = new Uint32Array( 0, 1)
var use = new Uint8Array(4, 16)
var Due = new Float32Array(20, 1)


console.log(id.BYTES_PER_ELEMENT)